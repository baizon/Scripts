#!/bin/bash

# # Don't forget to update '/etc/anacrontab' after changing path!
LOG=/var/log/trim.log

notify-send "Trim" "Trim started."
sudo sh -c "echo '*** $(date -R) ***' >> $LOG"
sudo sh -c "fstrim -v / >> $LOG"
sudo sh -c "fstrim -v /home >> $LOG"
notify-send "Trim" "Trim ended."
